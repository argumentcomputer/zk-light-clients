name: Rust

on:
  merge_group:
  push:
    branches:
      - "main"
  pull_request:
    types: [ opened, synchronize, reopened, ready_for_review ]
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always
  # Disable incremental compilation.
  #
  # Incremental compilation is useful as part of an edit-build-test-edit cycle,
  # as it lets the compiler avoid recompiling code that hasn't changed. However,
  # on CI, we're not making small edits; we're almost always building the entire
  # project from scratch. Thus, incremental compilation on CI actually
  # introduces *additional* overhead to support making future builds
  # faster...but no future builds will ever occur in any given CI environment.
  #
  # See https://matklad.github.io/2021/09/04/fast-rust-builds.html#ci-workflow
  # for details.
  CARGO_INCREMENTAL: 0
  # Allow more retries for network requests in cargo (downloading crates) and
  # rustup (installing toolchains). This should help to reduce flaky CI failures
  # from transient network timeouts or other issues.
  CARGO_NET_RETRY: 10
  RUSTUP_MAX_RETRIES: 10
  # Don't emit giant backtraces in the CI logs.
  RUST_BACKTRACE: short

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - buildjet-16vcpu-ubuntu-2204
        package:
          - "aptos"
      fail-fast: false
    env:
      RUSTFLAGS: -D warnings
    steps:
      - name: Set up git private repo access
        run: |
          git config --global url."https://${{ secrets.PRIVATE_PULL_TOKEN }}@github.com/".insteadOf ssh://git@github.com
          git config --global url."https://${{ secrets.PRIVATE_PULL_TOKEN }}@github.com".insteadOf https://github.com
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: taiki-e/install-action@nextest
      - uses: Swatinem/rust-cache@v2
      - name: Install deps
        run: |
          sudo apt-get update && sudo apt-get install -y pkg-config libudev-dev
      # make sure benches don't bit-rot
      - name: build benches
        run: cargo build --benches --release
        working-directory: ${{ github.workspace }}/${{ matrix.package }}
      - name: Run cargo test on aptos-lc-core
        run: |
          cargo nextest run -p aptos-lc-core --release --profile ci --all-features
        working-directory: ${{ github.workspace }}/${{ matrix.package }}
        env:
          RUSTFLAGS: "--cfg tokio_unstable"
      - name: Run cargo test on aptos-lc
        run: |
          cargo nextest run -p aptos-lc --release --profile ci --all-features
        working-directory: ${{ github.workspace }}/${{ matrix.package }}
        env:
          RUSTFLAGS: "--cfg tokio_unstable"
      - name: Doctests
        run: |
          cargo test --doc
        working-directory: ${{ github.workspace }}/${{ matrix.package }}

  clippy:
    runs-on: buildjet-16vcpu-ubuntu-2204
    strategy:
      matrix:
        package:
          - "aptos"
    steps:
      - name: Set up git private repo access
        run: |
          git config --global url."https://${{ secrets.PRIVATE_PULL_TOKEN }}@github.com/".insteadOf ssh://git@github.com
          git config --global url."https://${{ secrets.PRIVATE_PULL_TOKEN }}@github.com".insteadOf https://github.com
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install deps
        run: |
          sudo apt-get update && sudo apt-get install -y pkg-config libudev-dev
      # See '.cargo/config' for list of enabled/disabled clippy lints
      - name: rustfmt
        run: cargo fmt --all --check
        working-directory: ${{ github.workspace }}/${{ matrix.package }}
      - name: cargo clippy
        run: cargo xclippy -D warnings
        working-directory: ${{ github.workspace }}/${{ matrix.package }}
